'use strict';

// const Stream = require('stream').Duplex;
// const logUpdate = require('log-update');
const spinner = require('./');
const presets = require('./presets');

// let output = [];

// const stream = new Stream({
//   write(chunk, encoding, next) {
//     output.push(chunk);
//     next();
//   }
// })

const timer = spinner.start('Loading...');

setTimeout(() => {
  spinner.stop(timer);
}, 8000);
